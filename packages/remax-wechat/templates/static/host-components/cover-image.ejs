<template name="TPL_cover-image">
  <cover-image <% for (let i = 0; i< props.length; i++) { %>
    <%=props[i]%>="{{node.props['<%=props[i]%>']}}" <% } %>
  >
    <template is="TPL_TRAVERSAL" data="{{root: node}}" />
  </cover-image>
</template>