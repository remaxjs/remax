<template name="TPL_checkbox">
  <checkbox <% for (let i = 0; i< props.length; i++) { %>
    <%=props[i]%>="{{node.props['<%=props[i]%>']}}" <% } %>
  >
    <template is="TPL_TRAVERSAL" data="{{root: node}}" />
  </checkbox>
</template>