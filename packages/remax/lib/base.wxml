<template name="REACT_MINI_APP_TPL">
    <block wx:for="{{$$REACT_MINI_APP_ROOT}}" wx:key="">
        <template is="REACT_MINI_APP_TPL_1" data="{{item}}"/>
    </block>
</template>


<template name="REACT_MINI_APP_TPL_0">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_1" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_1">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_2" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_2">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_3" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_3">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_4" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_4">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_5" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_5">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_6" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_6">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_7" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_7">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_8" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_8">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_9" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_9">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_10" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_10">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_11" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_11">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_12" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_12">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_13" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_13">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_14" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>

<template name="REACT_MINI_APP_TPL_14">
    <block wx:if="{{item.text}}">
      <!-- string -->
      {{item.text}}
    </block>

    <block wx:if="{{item.type === 'view'}}">
        <view bind:touchstart="{{item.props.onClick || ''}}">
            <block wx:for="{{item.children}}" wx:key="">
              <template is="REACT_MINI_APP_TPL_15" data="{{item}}"/>
            </block>
        </view>
    </block>
  
</template>


