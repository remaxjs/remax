<template name="TPL_scroll-view">
  <scroll-view <% for (let i = 0; i< props.length; i++) { %>
    <%=props[i]%>="{{node.props['<%=props[i]%>']}}" <% } %>
  >
    <template is="TPL_TRAVERSAL" data="{{root: node}}" />
  </scroll-view>
</template>