<template name="TPL_picker">
  <picker <% for (let i = 0; i< props.length; i++) { %>
    <%=props[i]%>="{{node.props['<%=props[i]%>']}}" <% } %>
  >
    <view>
      <template is="TPL_DEFAULT" data="{{root: node.children[0]}}" />
    </view>
  </picker>
</template>
