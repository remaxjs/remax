<template name="TPL_input">
  <input <% for (let i = 0; i< props.length; i++) { %>
    <%=props[i]%>="{{node.props['<%=props[i]%>']}}" <% } %>
  >
    <template is="TPL_TRAVERSAL" data="{{root: node}}" />
  </input>
</template>