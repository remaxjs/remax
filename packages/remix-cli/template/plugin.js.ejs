import { PluginDriver } from '@alipay/remix-runtime';

export const pluginDriver = new PluginDriver([
<% pluginFiles.forEach(file => { %>
  require('<%- file %>'),
<% }) %>
].map(p => p.default || p));
